{% extends 'base.html.twig' %}

{% block title %}RoadTrip index{% endblock %}

{% block body %}

    <div class="flex justify-between items-center p-6">
        <h1 class="text-2xl">Mes RoadTrips</h1>
        <a href="{{ path('app_mes_road_trip_new') }}" class="px-4 py-2 rounded-lg bg-teal-600 text-white hover:bg-teal-500 transition-colors">
            Créer un nouveau RoadTrip
        </a>
    </div>

    {{ include('mes_road_trips/_content_index.html.twig') }}

{% endblock %}
